<h2>Job Number: <%= @service.id %></h2>

<p><%= @service.product %> on <%= @service.bike.brand %></p>
<p>Costs <%= number_to_currency(@service.price, unit: "£") %></p>

<%= link_to 'Update Details', edit_service_path(@service) %>
<%= link_to 'Delete Record', @service, method: :delete %>